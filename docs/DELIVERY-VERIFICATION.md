# V2 便携包交付校验

验证日期：2026-09-19。适用 Windows 10/11 x64；解压路径使用英文，可含空格。

## 交付文件

- 文件：`package/out-v2/stu-manage-portable-windows-x64.zip`
- 大小：516,371,770 字节，约 492.5 MiB。
- ZIP SHA-256：`592054944F6055FB3E49A01DC7900F1535513E2280708FEBB1C0947E9E23EEB2`
- 应用 JAR SHA-256：`BBDC5F8AABA2808950435D130D3186FDFCE5F57EC6F870A622FCDBD74FD12FAD`

本文件放在 ZIP 外，便于校验 ZIP；ZIP 内包含运行教程、技术演示步骤与代码学习笔记。后续重新构建的 ZIP 时间戳可能不同，SHA-256 也会变化，不应沿用本次校验值。

## 已完成的实际验证

1. ZIP 内含 Java 17、MySQL、Redis、VC++ 离线安装器、应用 JAR、启动/停止/检查/备份/恢复脚本及七份教程。检查未包含 `data`、`logs`、本地数据库密码、JWT 密钥和测试上传文件。
2. 从 ZIP 全新解压到 `D:\codex\practice3\portable final verify\stu-manage-portable`；首次启动自动建立独立数据库、生成凭据、创建演示数据，MySQL 查询、Redis PONG 与应用健康检查全部通过。
3. 三服务仅监听 `127.0.0.1` 的 `13306`、`16379`、`18090`；无需系统 Java、Node、Maven 或 Docker。
4. 干净数据只含五门初始课程；Redis 接口第二次读取真实命中。临时数据库引导 SQL 在认证验证成功后移除。
5. 重复运行启动脚本复用原进程；全停后三个端口释放；再次启动成功。删除一门未发布演示课程后重启，它不会重新出现，说明业务数据没有被初始化逻辑覆盖。
6. 同版本独立副本验证备份和恢复：SQL 中文字段、中文名媒体文件恢复后一致，文件 SHA-256 一致；恢复前自动备份当前状态，应用自动停启，MySQL/Redis PID 保持不变。
7. 浏览器验收通过管理员登录、技术页深链接刷新、Redis 命中/TTL/生成时间、SSE 连接、课程编辑限制、真实 PNG 上传预览发布与取消删除。损坏 PNG 返回 HTTP 400。
8. 后端 12 项测试全部通过，无失败、错误或跳过；跨角色 11 步完整业务 smoke 通过。详情见 [测试记录](TESTING.md)。

## 验证范围

这是同一台电脑上使用完整独立依赖、全新解压目录和全新数据库的验证，尚未在另一台实体电脑运行。目标电脑需 Windows x64 与 VC++ x64 运行库；缺少运行库时可使用包内离线安装器。天气接口需要互联网，离线教学业务不依赖天气。Docker 已通过配置解析，当前环境未运行容器。

启动与故障排查见 [便携版教程](V2-PORTABLE.md)，课堂展示见 [技术演示](TECH-DEMO.md)。
